# general config
model_class: 'buildings'
scenario: 'baseline'

# logging
logging:
  level: 'DEBUG' # options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# data sources
input_data_path: 'data/baseline_buildings/input'

# model customization
customization:
  lifetime_model_name: 'NormalLifetime'

# visualization
visualization:
  inflow:
    do_visualize: False

# data export
output_path: 'data/baseline_buildings/output'
do_export:
  pickle: False
  csv: True
